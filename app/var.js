import React from 'react';
import { StyleSheet, Dimensions } from 'react-native';

BackColorDrark = '#0d1117';
width = Dimensions.get('window').width;

function screenWidth() {
  return screenWidth = Dimensions.get('window').width;
}